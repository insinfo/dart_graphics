/// Pattern repetition modes
enum DartGraphicsPatternRepetition {
  /// Repeat in both directions (default)
  repeat,
  /// Repeat horizontally only
  repeatX,
  /// Repeat vertically only
  repeatY,
  /// No repetition
  noRepeat
}
